# lol-draft 프로젝트 지침

이 파일은 Codex가 `lol-draft` 프로젝트를 수정할 때 따라야 할 공통 지침이다.
프로젝트의 현재 코드와 데이터 구조가 이 문서의 설명보다 더 구체적이라면 기존 구조를 먼저 확인하고, 충돌하거나 판단이 어려운 경우 사용자에게 질문한다.

## 1. 프로젝트 목표와 기능 우선순위

이 프로젝트는 League of Legends 프로 경기의 밴픽 데이터를 수집·정리하고, 현재 밴픽 상황에 맞는 밴, 픽 및 조합을 추천하는 시스템을 만드는 것이 목표다.

### 기본 기능

다음 기능을 프로젝트의 핵심이자 최우선 개발 목표로 삼는다.

- 아직 아무 챔피언도 선택되지 않은 선픽 상황에서 현재 패치, 진영, 팀 성향과 메타 등을 고려해 적합한 첫 챔피언을 추천한다.
- 상대와 아군의 밴·픽이 진행된 상황에서는 지금까지 나온 모든 밴과 픽을 바탕으로 다음에 선택할 챔피언을 추천한다.
- 현재 상황에서 상대의 전략을 제한하거나 아군의 조합을 완성하는 데 효과적인 다음 밴을 추천한다.
- 개별 챔피언뿐 아니라 역할 구성, 시너지, 카운터와 전체 조합의 방향을 함께 고려해 조합을 추천한다.
- 현재 밴픽 상태를 바탕으로 상대 또는 아군이 다음에 선택할 가능성이 높은 챔피언을 예측한다.
- 다음 챔피언 예측 결과는 단일 챔피언만 제시하지 않고, 가능한 챔피언별 확률 또는 확률이 높은 순서의 후보 목록으로 제공할 수 있게 설계한다.

추천과 확률 예측은 밴픽의 각 단계에서 갱신되어야 한다. 모델의 입력에는 현재까지의 밴·픽 순서, 선택 가능한 챔피언, 진영, 패치, 대회 시점, 팀 및 선수 정보처럼 예측 시점에 실제로 알 수 있는 정보만 사용한다.

### 추가 확장 기능

다음 항목은 기본 추천·예측 기능이 안정화된 뒤 추가하는 확장 기능이다.

- 양 팀의 최종 픽과 조합을 보고 어느 팀의 조합이 더 유리한지 판단한다.
- 밴픽 과정 전체를 분석해 어느 팀이 밴픽 단계에서 더 유리했는지 판단한다.
- 현재 밴픽 상태 또는 최종 조합을 바탕으로 경기 승리 확률을 예측한다.
- 추천 결과와 실제 선택을 비교하고, 추천 이유를 시너지·카운터·역할 구성 등의 근거로 설명한다.

단순 챔피언 승률뿐 아니라 조합 간 상성, 역할 구성, 시너지, 카운터, 진영, 패치와 대회 시점 등을 모델의 특성으로 활용할 수 있게 한다. 학습용 데이터와 평가용 데이터를 명확히 나누고 데이터 누수 없이 모델 성능을 검증한다.

## 2. 현재 데이터 수집 방향

- 2025년 데이터는 기존 로컬 데이터를 그대로 사용한다.
- 2025년 데이터를 다시 자동 다운로드하거나 임의로 덮어쓰지 않는다.
- 2026년 데이터는 아래 Google Drive 공유 파일에서 자동으로 다운로드하도록 유지한다.
  - `https://drive.google.com/file/d/1hnpbrUpBMS1TZI7IovfpKeZfWJH1Aptm/view?usp=drive_link`
  - Google Drive file ID: `1hnpbrUpBMS1TZI7IovfpKeZfWJH1Aptm`
- Google Drive 다운로드에는 현재 `gdown`을 사용한다.
- `gdown.download()`에서 설치된 버전이 지원하지 않는 `fuzzy` 인자를 사용하지 않는다.
- 다운로드 파일이 이미 존재할 때의 동작은 기존 코드 정책을 먼저 확인한다. 정상 파일을 불필요하게 반복 다운로드하거나 덮어쓰지 않는다.
- 다운로드 실패, 파일 손상, 예상한 열 누락을 구분해 알기 쉬운 오류 메시지를 제공한다.

## 3. 밴픽 데이터 처리 규칙

- 밴 슬롯이 비어 있다고 해서 무조건 데이터 누락으로 판단하지 않는다.
- 팀이 밴 카드를 사용하지 않았거나 밴을 건너뛴 경우의 빈 값은 정상적인 밴픽 상태일 수 있다.
- 실제 데이터 누락과 의도적인 빈 밴을 구분할 수 있도록 원본 열, 밴 순서, 경기 형식과 기존 데이터 표현을 먼저 확인한다.
- 빈 밴을 허용하더라도 필수 픽, 경기 식별자, 팀 정보 등 분석에 반드시 필요한 값의 누락 검사는 유지한다.
- `failed_game` 또는 실패 목록을 만들 때는 최소한 해당 경기의 연도(2025 또는 2026), 경기 식별 정보와 실패 이유를 함께 기록한다.
- 가능하면 실패 원인을 다운로드 실패, 파싱 실패, 필수 데이터 누락, 형식 불일치 등으로 구분한다.
- 기존 CSV/JSON/Parquet 등의 열 이름, 자료형과 저장 형식은 명확한 이유 없이 변경하지 않는다.
- 데이터 형식을 변경해야 한다면 기존 파일과의 호환성 및 마이그레이션 영향을 먼저 확인한다.

## 4. 실행 환경과 운영체제 호환성

- 이 프로젝트는 Windows와 macOS에서 같은 Git 저장소를 사용한다.
- 코드가 두 운영체제에서 모두 동작하도록 작성한다.
- `/Users/...`, `C:\\Users\\...` 같은 운영체제별 절대경로를 코드에 하드코딩하지 않는다.
- 파일 경로는 문자열 결합 대신 `pathlib.Path`를 우선 사용한다.
- 경로의 기준은 현재 터미널 위치보다 `__file__`에서 계산한 프로젝트 루트 또는 코드에 정의된 프로젝트 루트를 우선한다.
- 줄바꿈, 파일 인코딩과 경로 구분자 차이 때문에 한 운영체제에서만 동작하지 않도록 주의한다.
- 텍스트 파일은 특별한 이유가 없으면 UTF-8을 사용한다.

## 5. Python 가상환경과 의존성

- 프로젝트 내부의 `.venv`를 사용해 실행하고 테스트한다.
- 가상환경 활성화에 의존하기보다 가능하면 현재 운영체제에 맞는 가상환경 Python을 직접 사용한다.
  - macOS: `.venv/bin/python`
  - Windows: `.venv\\Scripts\\python.exe`
- `.venv` 디렉터리는 Git에 커밋하지 않는다.
- 새 Python 패키지를 사용하는 변경을 하면 `requirements.txt`에도 해당 의존성을 추가하거나 기존 항목을 갱신한다.
- 전체 환경을 무분별하게 `pip freeze`로 덮어쓰기보다 프로젝트에 실제로 필요한 직접 의존성을 유지한다.
- `gdown`은 2026년 데이터 자동 다운로드에 필요한 프로젝트 의존성이다.
- 패키지가 없다는 이유만으로 코드에 임시 우회 로직을 추가하지 말고, 먼저 현재 가상환경과 `requirements.txt`를 확인한다.

## 6. 코드 수정 방식

- 수정 전에 프로젝트 구조와 관련 파일을 먼저 읽는다.
- 사용자가 요청한 부분만 필요한 범위에서 수정하고, 관련 없는 코드를 임의로 재작성하지 않는다.
- 기존 사용자 변경 사항과 데이터 파일을 보존한다.
- 전체 파일을 새로 덮어쓰기보다 가능한 한 필요한 부분만 수정한다.
- 동일한 로직을 여러 곳에 복사하지 말고 작고 명확한 함수로 분리한다.
- 변수명과 함수명은 역할을 알 수 있게 작성하고, 복잡한 데이터 판정에는 짧은 주석을 추가한다.
- 단순 오류를 숨기는 광범위한 `except Exception`은 피한다. 필요한 경우 오류 맥락을 기록하고 다시 발생시키거나 명확한 실패 결과를 반환한다.
- 사용자에게 코드를 다시 복사해 붙여넣으라고 하기보다 프로젝트 파일을 직접 수정한다.
- 변경 후에는 수정한 파일과 핵심 변경 내용을 요약해 알린다.

## 7. 검증 원칙

- 변경 후 프로젝트의 `.venv`를 사용해 관련 스크립트나 테스트를 실행한다.
- 데이터 전체 처리가 오래 걸리면 먼저 작은 표본이나 검증 모드로 확인하고, 가능한 범위에서 전체 실행 여부를 결정한다.
- 최소한 다음 경우를 확인한다.
  - 기존 2025년 로컬 데이터만 있는 경우
  - 2026년 데이터가 없어 자동 다운로드해야 하는 경우
  - 2026년 데이터가 이미 존재하는 경우
  - 의도적으로 밴을 사용하지 않아 밴 슬롯이 빈 경우
  - 실제 필수 정보가 누락된 경우
  - 다운로드에 실패한 경우
  - Windows와 macOS의 경로 처리
  - 아무 밴·픽도 없는 선픽 상황에서 추천하는 경우
  - 밴픽이 일부 진행된 각 단계에서 다음 밴과 픽을 추천하는 경우
  - 이미 밴되거나 선택된 챔피언이 추천 후보에서 제외되는지
  - 다음 챔피언 확률 예측의 후보 순서와 확률값이 유효한지
- 테스트 또는 실행을 할 수 없었다면 성공했다고 가정하지 말고, 실행하지 못한 이유와 사용자가 확인할 명령을 알려준다.
- 변경 전후의 `git diff`를 확인해 요청 범위를 벗어난 수정이 없는지 검토한다.

## 8. Git 작업 규칙

- `.venv/`, `__pycache__/`, `.DS_Store`와 생성된 임시 파일은 커밋 대상에서 제외한다.
- 데이터 파일은 크기와 저장소 정책을 확인한 뒤에만 Git 추적 여부를 변경한다.
- 사용자의 요청 없이 `git push`, 강제 푸시, 브랜치 삭제, 히스토리 재작성 또는 기존 변경 삭제를 하지 않는다.
- 원격 저장소와 충돌할 때 사용자 변경을 버리지 않는다.
- Windows와 macOS 사이에서는 먼저 원격 변경을 안전하게 가져오고 로컬 변경과 통합한 뒤 푸시하는 흐름을 따른다.

## 9. 앞으로의 개발 우선순위

1. 2025·2026년 데이터 수집과 통합 파이프라인을 안정화한다.
2. 의도적인 빈 밴과 실제 누락을 구분하고 데이터 품질 보고서를 만든다.
3. 각 경기의 밴픽을 단계별 상태와 다음 실제 행동으로 변환할 수 있는 학습 데이터를 만든다.
4. 선픽 추천, 진행 중인 밴픽의 다음 픽 추천, 다음 밴 추천에 사용할 입력 특성과 정답(label)을 정의한다.
5. 다음에 선택될 챔피언들의 확률 분포를 예측하는 단순하고 해석 가능한 baseline 모델을 만든다.
6. Top-1 accuracy뿐 아니라 Top-k accuracy, log loss 등의 지표로 다음 챔피언 예측 성능을 평가한다.
7. 이미 밴되거나 선택된 챔피언을 제외하는 등 각 밴픽 단계의 선택 가능 조건을 추천 결과에 적용한다.
8. 시간 순서와 패치를 고려해 학습·검증·테스트 데이터를 분리하고 데이터 누수를 방지한다.
9. 개별 밴·픽 추천에서 조합 전체 추천으로 확장한다.
10. 기본 추천·예측 기능이 안정화된 뒤 조합 우세, 밴픽 우세와 경기 승리 확률 판단 기능을 추가한다.

## 10. 사용자와의 소통

- 사용자는 수정된 전체 코드를 매번 복사·붙여넣는 방식을 피하려고 Codex를 사용한다.
- 요청이 명확하면 프로젝트를 직접 확인하고 수정·검증까지 진행한다.
- 기술적인 설명은 초보자도 따라갈 수 있도록 한국어로 명확하게 작성한다.
- 명령어를 안내할 때 macOS와 Windows의 차이가 있으면 둘을 구분한다.
- 중요한 설계 선택이나 데이터 손실 가능성이 있는 작업은 실행 전에 사용자에게 설명하고 확인한다.
- 결과 보고에는 다음 내용을 간단히 포함한다.
  - 수정한 파일
  - 무엇을 왜 변경했는지
  - 어떤 명령 또는 테스트로 검증했는지
  - 아직 남은 문제나 사용자가 해야 할 일이 있는지
